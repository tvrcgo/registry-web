
name: Deploy to github pages

on:
  push:
    branches:
      - dev
      - master

permissions:
  contents: write

jobs:
  gh-pages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build
        run: |
          export UMI_PUBLIC_PATH=/registry-web/public
          npm install
          npm run build
          mkdir dist/public
          mv dist/*.* dist/public/
          mv dist/public/*.html dist/

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          branch: gh-pages
